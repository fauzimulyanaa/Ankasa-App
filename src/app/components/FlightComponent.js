// FlightComponent.jsx
import React from "react";

const FlightComponent = ({ flight }) => {
  // Komponen FlightComponent di sini
  return (
    <div>
      <p>{flight.name}</p>
    </div>
  );
};

export default FlightComponent;
